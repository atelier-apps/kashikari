<div class="header">
<div class="filter-area">
</div>
</div>
<div class="list-area">
  <% @friends.each do |friend| %>
  <div class="list" onclick="edit_friend(<%=friend.id %>,`<%=friend.name %>`);">
    <div class="main">
      [<%=friend.id %>] <%=friend.name %> | 最終更新 <%=parse_time(friend.updated_at) %>
    </div>
  </div>
  <% end %>
</div>
